class Person{
    name:string='charles';
}
const person =new Person();
person.name="Alex";

console.log(person);

// The members of a calss (properties & methods) are typed using
// type annotations, similar to variables.
